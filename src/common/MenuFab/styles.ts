import { Fab } from '@material-ui/core'
import styled from 'styled-components'

const MenuFab = styled(Fab)`
  display: flex;
  justify-content: center;
  align-items: center;
`

const Styled = { MenuFab }

export default Styled
