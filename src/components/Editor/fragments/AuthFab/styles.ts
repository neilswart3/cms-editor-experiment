import { Fab } from '@material-ui/core'
import styled from 'styled-components'

const AuthFab = styled(Fab)`
  position: absolute;
  top: 1rem;
  right: 1rem;
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 100;
`

const Styled = { AuthFab }

export default Styled
