import { ListItem } from '@material-ui/core'
import styled from 'styled-components'

const NavItem = styled(ListItem)`
  display: block;
  padding: 0.75rem 1rem;
  text-align: center;
`

const Styled = { NavItem }

export default Styled
