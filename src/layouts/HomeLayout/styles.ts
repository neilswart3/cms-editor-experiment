import styled from 'styled-components'
import GeneralLayout from '../GeneralLayout'

const HomeLayout = styled(GeneralLayout)`
  display: block;
`

const Styled = { HomeLayout }

export default Styled
